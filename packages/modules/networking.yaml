defaults:
  networking: ${ networking | default({}) }
  networking_mode: ${ networking.get("mode", "wifi") } # wifi | ethernet | none

packages:
  - !include networking/${networking_mode}.yaml

api:
  id: hapi

ota:
  - platform: esphome

mdns:
  disabled: false
