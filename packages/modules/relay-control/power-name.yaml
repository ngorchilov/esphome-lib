defaults:
  rc: ${ rc | default({}) }
  rc_power: ${ rc.get("power", {}) }

  rc_id: ${ rc.get("id", "rc1") }
  relay_name: ${ rc_power.get("name", "Power") }

switch:
  - id: !extend ${rc_id}_power_relay
    name: ${relay_name}
