defaults:
  receiver: ${ receiver | default({}) }
  receiver_enabled: ${ receiver.get("enabled", false) }
  _receiver_core_pkg: !include remote-receiver-rc-switch/core.yaml

packages:
  - ${ receiver_enabled and _receiver_core_pkg or {} }
