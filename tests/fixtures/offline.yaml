substitutions:
  name: test-offline
  board_vendor: espressif
  board: esp32dev
  test_networking: {mode: none}
packages:
  - !include
    file: ../../packages/boards/${board_vendor}/${board}.yaml
    vars:
      networking: ${test_networking}
