substitutions:
  name: test-explicit-wifi
packages:
  - !include
    file: ../../packages/boards/espressif/esp32dev.yaml
    vars:
      networking:
        wifi:
          ssid: test-network
          password: test-password
