substitutions:
  name: test-disabled-basic
  role: switch
  entity_enabled: false
packages:
  - !include
    file: ../../devices/sonoff-basic-r4.yaml
    vars:
      entity:
        enabled: ${entity_enabled}
        type: ${role}
